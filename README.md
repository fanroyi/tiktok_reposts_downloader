# TikTok Reposts Downloader

Scrape all reposted TikTok video URLs from a profile and download them (no watermark) via ssstik.

## Quick Start

1) Install deps:
```bash
pip install -r requirements.txt
```

2) Run:
```bash
python main.py 
```


